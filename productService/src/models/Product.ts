export interface Product {
	id: string;
	title: string;
	count: number;
	description: string;
	price: number;
}

export interface ProductDto {
	title: string;
	count: number;
	description: string;
	price: number;
}
